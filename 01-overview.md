# 从启动和恢复看懒加载

> 先理解我们希望用户获得什么，再看三个项目如何协作。这里是设计建议，不是已完成功能清单。

## 一个具体场景

用户在 Conch 里创建一个 Python 沙箱。镜像里可能有大量依赖，但启动脚本只使用一小部分。全部下载后启动，网络会进入关键路径；按需下载可以提前运行，但第一次使用某个库时仍可能等待。

用户随后保存快照，再从它恢复。除了镜像文件，还需要恢复**运行中写入的文件、进程内存以及设备状态**。只解决镜像下载，不能保证快照也能快速恢复。

所以目标不是“让 lazyd 下载得更聪明”，而是：

**让 Conch 更早交付可用沙箱，同时保证读对数据、写入隔离、故障可处理、资源可回收。**

## 先分清三类数据

| 数据 | 例子 | 恢复时必须保证 |
| --- | --- | --- |
| 镜像提供的只读文件 | Python 解释器、依赖库 | 引用确定版本；相同内容可在授权范围内共享 |
| 运行时写入的磁盘数据 | 新建文件、包安装结果、数据库文件 | 读到最新历史版本；新写入归当前沙箱 |
| Guest RAM，即虚机内存 | 正在执行的进程、堆、页表 | 按快照恢复；恢复后的写入不能被迟到数据覆盖 |

设备和 CPU 状态也要匹配，但并不是可任意延迟读取的普通数据块。快照一致性必须覆盖它们。

三类对象可以使用同一个内容服务的不同适配器，也可以由不同后端处理。**统一服务不等于混用身份和写入语义。**

## 三仓共同负责一次操作

- **Conch**：选定镜像或快照，决定每类资源的准备策略，登记引用，协调启动、失败和回收。
- **StratoVirt**：把资源接到虚机设备和内存，执行地址映射、运行控制和必要的缺页完成动作。
- **lazyd**：提供可复用的内容获取、缓存、校验与调度；是否承接外部 UFFD handler、磁盘历史块或内存来源，由具体选型决定。
- **guestd**：Conch 仓里的 guest agent，在虚机内识别设备和组装挂载，不是第四个独立产品。

先看[三仓协作图](04-conch-design-reference/assets/target-responsibilities.html)，再看[一次冷启动](05-flows/01-cold-rootfs-lazy-start/README.md)。

## 我们目前倾向什么

| 问题 | 建议起点 | 什么可能改变选择 |
| --- | --- | --- |
| 只读 rootfs | 优先验证 EROFS + pmem/DAX 的文件页共享；同时保留整盘块路径作对照 | guest 支持、设备规模、完整恢复成本、PSS 和业务延迟 |
| 缺页处理位置 | 外部 handler 与 VMM 内置 handler 对比；先验证上游外部 UFFD 的可复用性 | 故障隔离、权限、协议往返和快照交接复杂度 |
| 数据服务组织 | 共用内容/下载/预算基础设施，按镜像、磁盘、内存区分适配器 | 格式兼容、已有后端能力及错误影响范围 |
| 预取与提前映射 | 有界按需为基线，分别验证工作集预取、缓存范围提前映射 | 网络条件、预取命中率、启动额外等待 |
| 回收 | Conch 管运行引用，内容服务管缓存存储；明确跨服务租约或等价保护 | 多节点、重启恢复、容量与凭据隔离 |

这是有优先级的验证方向，**不是已经冻结的协议或部署拓扑**。选型理由和第一方证据见[八个设计问题](04-conch-design-reference/08-design-decisions-and-evidence.md)。

## 哪些要求不能妥协

1. 未下载的数据不能伪装成零；增量层“继承父层”和“显式清零”必须可区分。
2. 可复用的持久 ready 记录不能先于对应数据的持久化。
3. 共享内容不可被任意 VM 写坏；可写磁盘和运行内存保持正确隔离。
4. 数据源未能处理请求时不能让 VM 永久无反馈等待。
5. 删除一个沙箱不能使其他沙箱丢失数据；启动失败不能发布虚假的可用状态。
6. 宣称性能提升之前，要测业务，而不只测 pull 或 vCPU resume。

## 哪些已经在上游，哪些还不是

[基线核查](04-conch-design-reference/01-current-system-boundary.md)记录了本次查询的 commit 和源码位置。Conch 已有原生资源 store、启动布局和 StratoVirt 接入；StratoVirt 已有普通 pmem 与内存恢复的外部 UFFD 机制。不能据此推出 rootfs 远端缺页或增量恢复全链路已经具备。

产品研究覆盖 Nydus、E2B、OverlayBD、Firecracker、Cloud Hypervisor、QEMU、CRIU、Kata，以及独立项目；机制事实保留各自核查版本，不把提案当作已发布能力。

## 接下来怎么读

- 想参与设计：读[候选方案与选择门槛](04-conch-design-reference/02-adopt-adapt-reject.md)。
- 想解释快照：读[一次 checkpoint 的保存与恢复](04-conch-design-reference/06-checkpoint-three-path-restore.md)。
- 想开始开发：读[联合实施路线](04-conch-design-reference/07-phased-roadmap.md)。每个阶段都包含三仓联调，不按仓库先后串行堆功能。
