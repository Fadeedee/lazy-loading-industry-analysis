# 文档撰写与维护原则

> 阅读完成后，作者和评审者能够按一致结构补充产品调研、解释设计依据，并检查证据、实现状态与图文是否一致。

本指南规定本仓库的研究、方案设计和可读性要求。适用于人工和 AI 辅助维护；只克隆本仓库即可使用，无需访问作者机器上的设计文件或聊天记录。

## 读者与表达

- 正文以中文写作，项目名、系统调用、协议字段和源码符号保留原文。
- 标题后用引用块说明读者读完能解决什么问题；避免只写“介绍某项目”。
- 先说具体对象和访问过程，再解释抽象：谁发起、谁阻塞、谁取数、数据放到哪里、谁完成请求。
- 一个小节解释一个主要问题；新术语首次出现时说明含义，并按需链接到 [术语表](appendix/glossary.md)。
- 保留影响实现的条件、限制和失败路径，避免无依据的“更快”“更先进”“已支持”。
- 内容按技术问题组织，不保留“上次我们讨论过”等对话过程，也不要求读者通过聊天记录补齐上下文。

## 设计不被既有实现绑定

- Conch、StratoVirt、lazyd 都是可共同修改的项目，不把本仓库写成 lazyd 单方扩展指南。
- 联合设计不等于三仓同等扩张。StratoVirt 只补必要的设备、地址空间、页完成和运行控制能力；内容与策略留在 Conch/数据源。候选实验不累加成首版功能，统一框架不是默认前置，边界以 [StratoVirt 职责](04-conch-design-reference/05-stratovirt-responsibilities.md)为准。
- 从 prepare、create/restore、访问、checkpoint、stop/GC 等用户场景推导职责，再选择进程边界。
- 实施时以各仓最新 upstream/dev 为基线并固定 commit；文档核查日期不能代替开工前核查。
- 不默认继承某个 ABI、handler 位置、缓存格式或“不做某优化”的限制；保留正确性约束，对候选列出选择条件与验证门槛。
- 复用经过核查的算法、测试和事实，不以旧分支补丁结构作为新架构依据。不在正文反复介绍历史设计。
- “复用现有能力”是审计后的结论，不是要求原有状态归属、API 或缓存格式保持不动。尤其不能把 lazyd 简化为既定的 FETCH 服务，再把所有新增要求推给 Conch/StratoVirt；需要时可以重组内容、来源、会话、任务和 I/O 边界，但也不为扩展性预建无需求的框架。
- 当前只读 rootfs 主线为 EROFS + pmem/DAX；可写层和后续快照来源分别讨论。业界整盘块路径可以作为对照，不因此增加一条首版开发主线。
- 阅读入口只呈现当前建议和关键分歧；源码细节用链接或折叠块承接，避免每页重复整条链路。

## 目录职责

服务模型与扩展规则以 [lazyd 专题](06-lazyd-design/README.md)为准：Conch 是调用方之一，产品语义不进入供数核心；StratoVirt 改动收敛。影响行为的参数显式定义，labels/extensions 只承载允许的调用方元数据。外部 handler 的主方案与验证状态统一维护在核心选型登记中。

| 位置 | 写什么 |
| --- | --- |
| `README.md` | 阅读入口、范围、图形导航 |
| `01-overview.md` | 关键结论和决策索引，链接详细分析 |
| `00-concepts/` | 对象、地址、触发、共享和快照的概念基线 |
| `02-products/` | 产品实际路径、版本限制及面向本项目的独立建议章节 |
| `03-design-comparison/` | 同一问题下的横向比较和取舍条件 |
| `04-conch-design-reference/` | 本项目职责、设计决策、与业界实现的对应关系 |
| `04-conch-design-reference/00-overall-design.md` | 纯方案阅读入口，连贯讲目标、架构、流程和边界；不展开业界比较或源码审计 |
| `05-flows/` | 前置资源、正常/失败流程、缓存命中和验收观察点 |
| `06-lazyd-design/` | 独立服务模型、扩展、运行与缓存约束、调度研究；不重复维护选型状态 |
| `appendix/` | 来源记录、成熟度矩阵和术语 |

每个主题保留一个详细说明位置，其他文档通过摘要和相对链接引用。产品内部实现较长时，放入对应产品的 `01-data-path.md` 或 `02-cache-snapshot-lifecycle.md`，避免多处复制后逐渐不一致。

纯方案版是现有设计的综合说明，不另立一套决策或实现状态。正文保留必要的待验证边界，证据与细化文档集中链接在末尾；下节的证据模板用于对应决策详解，不要求在纯方案版重复展开。职责或流程变化时同步检查两者一致性。

## 设计依据的固定模板

新增或调整本项目方案时，采用 [设计决策与业界依据](04-conch-design-reference/08-design-decisions-and-evidence.md) 的结构：

1. **场景与建议**：用户要完成什么操作，当前建议及可比较的替代方案是什么。先定角色和完成条件，再讨论仓库、进程与接口。
2. **参考项目与直接链接**：给出相关官方文档章节、源码文件/符号或 PR；不要只放项目首页或 Source ID。
3. **对方具体做了什么**：展开触发、定位、取数、缓存/校验、填充和完成路径；与本项决策无关的产品功能可省略。
4. **三仓怎样协作**：分别说明 Conch、StratoVirt、lazyd 可以调整什么，哪些只是候选、为何不同于参考项目。使用同一 syscall 不代表架构相同。
5. **状态与验证**：区分对方成熟度、我们的实现状态、已做实验及尚需验证的条件。
6. **进一步阅读**：链接产品详解、横向比较或流程图，便于读者继续追到代码。

“对方具体做了什么”至少提供足以理解本项取舍的摘要，并链接详解；不能只有“参考 Nydus”“类似 E2B”。本项目自行推导的策略直接标为设计建议，不为每项建议强行寻找来源项目，也不声称对方实现了相同策略。

## 数据服务调研不能只看触发入口

参考[数据服务横向比较](03-design-comparison/08-data-service-architecture.md)，对相关产品进一步追问：

- 逻辑地址怎样定位到确定内容，身份与来源凭据是否分开？
- 两个调用者请求同一缺失范围时，谁拥有任务，其他人如何等待、取消或收到失败？
- 下载、校验、写入、持久化、ready 发布分别何时完成？
- 前台需求与后台预取如何竞争，连接、队列、内存和并发是否有界？
- 准备就绪和运行完成分别指什么，谁可以关闭来源或回收缓存？
- 共享范围、权限和实际映射的生存期是否一致？

按产品的实际职责选择问题，不强求每个 VMM、格式提案或原型都实现完整缓存服务。没有查到源码就写“未核查/不能由该来源推出”，不要用成熟项目的名字填补缺失证据。

每个产品借鉴页应说明具体受启发的行为及三仓分工。表格不是要求三仓各加一组模块；某仓只需验证已有行为时明确写出。新增源码核查补到产品详解和来源清单，联合设计通过摘要链接它们，避免复制多份事实。

## 证据与时效

- 分别标注外部项目、内核原语、自有实现和设计推断的来源与适用范围。
- 借鉴关系需有具体出处；定位索引、缓存状态、任务状态和 VM 映射按各自职责解释。
- 各候选使用相同的选型标准，明确正确性门槛、验证状态及实现和维护成本。
- 正文直接陈述技术结论、理由与边界，不记录对话问答、纠错过程或任务授权；编辑和验证记录放在附录。
- 核心选型状态统一记录在 [核心设计选型与验证计划](04-conch-design-reference/09-core-design-selection.md)。整体方案与图是条件说明，不能反过来作为该候选已定案的证据。

- 技术事实使用第一方资料：官方源码、设计文档、规范、PR/issue、发布说明或作者论文。
- 在 [来源清单](appendix/source-inventory.md) 登记唯一 Source ID、链接、实际核查日期、commit/tag、状态及所支持的结论；引用放在相关段落附近。
- 代码细节优先链接固定 commit 的文件和符号/行号。使用移动分支链接时，注明核查版本；本地路径只能作辅助定位，不能作为远端读者唯一证据。
- 区分“最后核查事实的日期”和“编辑建议的日期”。只整理文字或复用旧调研时，不把来源日期更新为今天，也不宣称已经核查最新代码。
- PR 合入、版本发布等可变结论在重新决策前核对；无法核实时保留原核查日期并标明限制。
- 未由代码或文档确认的行为写为推断或未知。发布说明可证明宣布了某项能力，但不自动证明协议、权限和错误处理细节。
- 成熟度使用 [矩阵定义](appendix/maturity-matrix.md)：`released`、`merged`、`experimental`、`open-pr`、`closed-unmerged`、`prototype`、`proposal`。按具体能力记录，不能用项目某项成熟能力替另一项实验能力背书。

## 必须区分的技术语义

| 容易混淆的表述 | 写作时需要交代 |
| --- | --- |
| 懒加载、懒恢复 | 延迟的是远端下载、本地读盘、物理页填充还是地址映射 |
| rootfs、快照 | 只读 lower、历史磁盘层、当前 writable head、Guest RAM、设备状态分别是什么 |
| 缓存共享 | 下载去重、磁盘缓存复用、host page cache 和最终物理页共享分别是否成立 |
| guest page、page cache | 地址空间、RAM/pmem 区域、host/guest page cache 的具体归属 |
| Copy、COW、mmap | 谁执行、源/目标内存、映射权限和写入后行为；COW 不等于懒下载 |
| fault、下载、校验、映射范围 | 各自粒度及边界，不把常见 4 KiB 页或示例 unit 写成固定要求 |
| prefetch、prefault/PROBE | 内容 ready 与某个 VM 的 HVA 已映射是不同状态 |
| 内容身份、运行实例 | digest、逻辑视图、snapshot lineage、sandbox ID 与 attachment/租约分别管理什么 |

产品事实与我们的建议分别写。fanotify 不是本次 pmem/DAX 候选的 fault 入口；产品调研中可保留其实际用途，但不要因此加入我们的目标架构。任何机制都需要适用场景和验证依据。

## 图形与性能证据

- 架构图表示角色或组件职责，时序图表示交互顺序，状态图表示状态及迁移条件；标明现状/候选，以及是否为角色级示意。角色不一定对应独立进程，示例消息不等于已冻结的跨仓协议。
- 图和解释相互链接；采用相对路径，HTML、JSON 源文件放在对应 `assets/`，离线 HTML 不依赖远程脚本或字体。
- 修改图形源后重新生成 HTML。Archify 使用 `showcase` 校验并保存交付证据；未涉及图形语义或源文件的文字维护，无需重生成所有图。
- 分别报告静态校验、浏览器检查与实际截图审阅结果。缺少浏览器时写清跳过原因，不把静态通过当作视觉通过。
- 性能数字标注来源、版本、工作负载、冷/热缓存、网络和测量起止点；来源基准、本地实测、示例数字与预期收益分别标识。
- 对懒加载至少考虑应用可用时间、首次业务请求、尾延迟、下载量/放大以及共享收益；不能只凭 VM 提前 resume 就认定用户体验更快。

## 更新与提交

修改设计时，检查总览、对应产品建议、横向比较、分仓职责、流程图和开发路线是否需要同步；只更新受影响部分。状态变化同时更新来源清单和成熟度矩阵，保留版本差异的必要说明。

提交前确认：

- 新增/修改的本地链接可解析，Source ID 均已登记，直接链接支持对应事实。
- 目标、已实现、历史兼容、未来建议和未知项没有混写。
- 对方实现摘要和本项目取舍都能独立理解；关键差异、失败条件、验证边界没有被省略。
- `git diff --check` 通过，无临时占位符；图形有变化时完成相应校验。
- 只提交本次文档、图形源和交付物；不夹带产品代码、第三方 clone、凭据或临时构建输出。
- commit 按逻辑组织，例如范围澄清、设计依据说明、写作规范各自可以独立解释；push 遵循维护者本次授权。

纯文字维护不要求运行 Conch、lazyd 或 StratoVirt 的代码测试。需要报告的是实际做过的文档检查，以及任何影响结论的未验证条件。
